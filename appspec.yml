version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/admin
hooks:
  ApplicationStart:
    - location: common_utilities/scripts/after_deploy_supervisor_restart.sh
      timeout: 500
      runas: root